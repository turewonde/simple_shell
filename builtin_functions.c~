#include "shell.h"
/**
* print_env - prints all the environment variables
*
*Return: Void
*/
void print_env(void)
{
int i, j;
char *s = *environ;
for (i = 1; s != NULL; i++)
{
j = 0;
while(s[j])
{
write(1, &s[j], 1);
j++;
write(1, "\n", 1);
s = environ[i];
}
}
